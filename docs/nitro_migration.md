Act as an expert DevOps and Fullstack Developer specializing in Vite, React, and TanStack Start.

Context:
I am deploying a TanStack Start application to a VPS using Docker and Nginx. Currently, the HTML loads perfectly, but all client-side assets (like /assets/main.js and CSS) are returning a 404 Not Found, resulting in a white screen.

The Root Cause (MAYBE):
I am currently using a custom server.mjs powered by Hono (@hono/node-server) to serve the app. However, this custom server only proxies the server.fetch SSR logic and fails to serve the static assets generated by Vite in the dist/client folder.

The Goal:
I want to completely remove my custom Hono server.mjs and migrate to TanStack Start's default production server powered by Nitro. According to the official TanStack docs for Node.js/Docker deployments, Nitro automatically handles both SSR and static asset serving perfectly out of the box. Docs: https://tanstack.com/start/latest/docs/framework/solid/guide/hosting

Make sure (in my mind):
1. updated Dockerfile (Stage 3 runner needs to copy the .output folder generated by Nitro instead of dist, and stop copying server.mjs).

2. updated package.json start script (it should run node .output/server/index.mjs)?

3. updated docker-entrypoint.sh to execute the new Nitro server entry point?

4.  safely delete server.mjs and uninstall the @hono/node-server and hono dependencies.?